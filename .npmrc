# Use exact versions for production dependencies
save-exact=false

# Always run audit for security
audit=true

# Use ~ for minor version updates (safety delay buffer)
# New versions are available after 1 week due to npm caching
# Updates are applied within minor versions (patch releases)
save-prefix=~
